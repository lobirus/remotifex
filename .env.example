# Remotifex Configuration
# Copy to .env and fill in values
# All settings (domains, AI keys, etc.) are configured via the web UI.

# Security - CHANGE THESE IN PRODUCTION
JWT_SECRET=change-me-to-a-random-string
ENCRYPTION_KEY=change-me-to-a-random-32-byte-key

# MongoDB
MONGODB_URL=mongodb://mongo:27017/remotifex

# Redis
REDIS_URL=redis://redis:6379/0

# Data directory (inside containers)
PROJECTS_DATA_DIR=/data/projects
